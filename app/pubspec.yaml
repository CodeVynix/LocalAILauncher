name: local_ai_launcher
description: "Cross-platform local AI model launcher with on-device inference."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  fllama:
    git:
      url: https://github.com/Telosnex/fllama.git
      ref: main
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  device_info_plus: ^13.2.0
  file_picker: ^12.0.0
  path_provider: ^2.1.5
  path: ^1.9.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
